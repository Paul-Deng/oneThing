server:
  port: 9900
spring:
  application:
    name: user-service
  datasource:
    driver-class-name: org.h2.Driver
  flyway:
    enabled: true
    encoding: UTF-8
    locations: classpath:db/migration
    validate-on-migrate: true