server:
  port: 9000
spring:
  application:
    name: gateway
  profiles:
    active: dev
  cloud:
    nacos:
      username: nacos
      password: nacos
      server-addr: localhost:8848
      config:
        file-extension: yaml
        group: DEFAULT_GROUP
        namespace: public
