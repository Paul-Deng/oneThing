server:
  port: 9900
spring:
  application:
    name: user-service
  profiles:
    active: dev
  cloud:
    nacos:
      username: nacos
      password: nacos
      server-addr: localhost:8848
      config:
        file-extension: yaml
        group: DEFAULT_GROUP
        namespace: public
